# Atlas backend

The backend package contains the FastAPI control plane, PostgreSQL-backed frontier scheduler, Redis/RQ worker entrypoint, crawl pipeline, extraction, and OpenSearch adapter. See the repository root README for setup and architecture documentation.

